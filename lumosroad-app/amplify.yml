version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
        - npm install -g @expo/cli
    build:
      commands:
        - expo export --platform web --output-dir dist
  artifacts:
    baseDirectory: dist
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
